{
  "name": "dranken-lijst",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync:catalog": "node scripts/sync-catalog.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
